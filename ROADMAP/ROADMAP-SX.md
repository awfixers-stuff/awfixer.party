## Section X — Analytics and observability (Backboard + Admin)

In-house analytics: **max non-PII behavioral data** as defined with counsel; **admin-only** product use (`WORK.md` Q14); **no** separate product analytics on partysite as a goal.

### Subsection X.A — Ingest and dashboards

#### Phase X.A.1 — Events and dashboards

##### Subphase X.A.1.i — `admin.awfixer.party/analytics/**`

- [ ] Define event schema (clicks, session time, regions, form abandonment, per-field timing).
- [ ] Ingest via Backboard endpoints into Convex; build admin UI.
- [ ] **Create or update an external TODO list** with alerting, anomaly detection, and export for counsel.
